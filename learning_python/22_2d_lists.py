
number_grid = [ #in this 2d list we have 4 elements. each of them are also lists. 
    [1, 2, 3], #4 rows, 3 columns. we can create grid like structures in python using 2d lists. 
    [4, 5, 6],
    [7, 8, 9],
    [0]
]

print(number_grid[0][0]) #this prints 1, you put the index of the number you want to print. 
                        #number 1 is at 0th column 0th row. so we put [0][0]. play around with indexes for different results.

